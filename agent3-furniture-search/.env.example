# Agent 3 - Furniture Search (port 8003)
# Copy this file to ".env" in this folder and fill in your own values.
# Never commit .env (it is already in .gitignore).

# Same database as agents 1 and 2 (use the SAME value in all three).
# After the first start, load the product catalog once:
#   python import_csv.py real_products.csv
DATABASE_URL=postgresql://postgres:your-db-password@localhost:5432/interior_design
